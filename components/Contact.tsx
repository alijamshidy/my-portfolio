import { GitFork, Mail, Network } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Textarea } from "@/components/ui/textarea"
import { personalInfo } from "@/lib/data"
export default function Contact() { return <section id="contact" className="py-24"><div className="mx-auto grid max-w-6xl gap-10 px-5 lg:grid-cols-[.8fr_1.2fr]"><div><p className="text-sm font-medium text-primary">GET IN TOUCH</p><h2 className="mt-3 text-4xl font-semibold tracking-tight">Have a project in mind?</h2><p className="mt-5 max-w-md leading-7 text-muted-foreground">I&apos;m open to freelance projects and collaborative opportunities. Send a message and let&apos;s build something useful.</p><div className="mt-8 grid gap-3"><a href={`mailto:${personalInfo.email}`} className="flex items-center gap-3 text-sm hover:text-primary"><Mail className="size-4" />{personalInfo.email}</a><a href={personalInfo.github} target="_blank" rel="noreferrer" className="flex items-center gap-3 text-sm hover:text-primary"><GitFork className="size-4" />github.com/alijamshidy</a><a href={personalInfo.linkedin} target="_blank" rel="noreferrer" className="flex items-center gap-3 text-sm hover:text-primary"><Network className="size-4" />linkedin.com/in/alijamshidy</a></div></div><Card><CardHeader><CardTitle>Send a message</CardTitle></CardHeader><CardContent><form className="grid gap-5" action={`mailto:${personalInfo.email}`} method="post" encType="text/plain"><div className="grid gap-5 sm:grid-cols-2"><Input name="name" placeholder="Your name" required /><Input name="email" type="email" placeholder="Email address" required /></div><Input name="subject" placeholder="Project subject" required /><Textarea name="message" placeholder="Tell me a little about your project..." rows={6} required /><Button type="submit">Send inquiry <Mail /></Button></form></CardContent></Card></div></section> }
