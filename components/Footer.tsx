import { GitFork, Network } from "lucide-react"
import { personalInfo } from "@/lib/data"
export default function Footer() { return <footer className="border-t py-7"><div className="mx-auto flex max-w-6xl flex-col gap-4 px-5 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between"><p>© {new Date().getFullYear()} {personalInfo.name}. Built with care.</p><div className="flex gap-4"><a aria-label="GitHub" href={personalInfo.github} target="_blank" rel="noreferrer"><GitFork className="size-4" /></a><a aria-label="LinkedIn" href={personalInfo.linkedin} target="_blank" rel="noreferrer"><Network className="size-4" /></a></div></div></footer> }
