import { ArrowUpRight, GitFork } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardFooter, CardHeader, CardTitle } from "@/components/ui/card"
import { projects } from "@/lib/data"
export default function Projects() { return <section id="projects" className="py-24"><div className="mx-auto max-w-6xl px-5"><div className="mb-12 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-primary">SELECTED WORK</p><h2 className="mt-3 text-3xl font-semibold tracking-tight sm:text-4xl">Projects built to solve real problems.</h2></div><a href="https://github.com/alijamshidy" target="_blank" rel="noreferrer" className="inline-flex h-7 items-center gap-1 self-start rounded-md border px-2 text-xs hover:bg-input/50">View GitHub <GitFork className="size-3.5" /></a></div><div className="grid gap-5 md:grid-cols-2">{projects.map(project => <Card key={project.id} className="gap-0 overflow-hidden transition-shadow hover:shadow-xl"><div className={`h-40 bg-gradient-to-br ${project.accent} p-5`}><div className="flex h-full items-end justify-between rounded-lg border border-white/25 bg-black/10 p-4 font-mono text-xs text-white/90"><span>/{project.name.toLowerCase().replaceAll(" ", "-")}</span><ArrowUpRight /></div></div><CardHeader><CardTitle className="text-base">{project.name}</CardTitle></CardHeader><CardContent><p className="min-h-12 text-sm leading-6 text-muted-foreground">{project.description}</p><div className="mt-5 flex flex-wrap gap-2">{project.tech.map(tech => <Badge key={tech} variant="secondary">{tech}</Badge>)}</div></CardContent><CardFooter className="mt-auto"><a href={project.github} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-xs font-medium text-primary hover:underline">Source code <ArrowUpRight className="size-3.5" /></a></CardFooter></Card>)}</div></div></section> }
